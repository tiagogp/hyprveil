# NVIDIA profile: intentionally conservative. Selecting this profile does not
# enable WLR_DRM_NO_ATOMIC, cursor, VA-API, or GLX overrides. Enable an individually
# justified workaround manually only after reading docs/HARDWARE.md.
